@model IEnumerable<Gaming_Store.Models.Jogos>
@{
    ViewBag.Title = "AdminView";
}

<h2>AdminView</h2>
<p>
    @Html.ActionLink("Adicionar Jogo", "Create")
</p>
@* Displayname-> nome do conteudo *@
@foreach (var item in Model)
{
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Preco)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fotografia)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fotografia)
                <a href="~/Fotografias/" target="_blank">
                    <img src="~/Fotografias/" width="50" height="37" />
                </a>
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Detalhes", "Details", new { id = item.Id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
               

            </td>
        </tr>
    }
</table>
